<% @tweets.each do |tweet| %>
    <br>
        <p>
            <%= tweet.message %>
        </p>
            <%= (((Time.current) - (tweet.tdate)).div(60)) %>
            <%= '分前の投稿:  ' %>
        <%= link_to '削除', "/tweets/#{tweet.id}", method: :delete %>
        <%= link_to '表示', "/tweets/#{tweet.id}"" %>
    </br>
    
<% end %>
<p><%= link_to '新規追加', '/tweets/new' %></p>
